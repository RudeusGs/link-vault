spring:
  datasource:
    url: jdbc:h2:mem:linkvault_test;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
    show-sql: false
  flyway:
    enabled: false

app:
  seed-demo-data: false
  jwt:
    secret: test-jwt-secret-that-is-long-enough-for-hmac



  redis:
    enabled: false
    rate-limit:
      enabled: false
  rabbitmq:
    enabled: false
